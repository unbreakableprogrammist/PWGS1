<UserControl x:Class="Libraria.Views.ReaderView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d"
             d:DesignHeight="560"
             d:DesignWidth="1000">
    <Grid Background="#F7F8FA">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <Border Grid.Row="0"
                Background="White"
                BorderBrush="#D8DDE6"
                BorderThickness="0,0,0,1"
                Padding="14,10">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto" />
                    <ColumnDefinition Width="*" />
                    <ColumnDefinition Width="Auto" />
                </Grid.ColumnDefinitions>

                <Button Grid.Column="0"
                        Content="Wstecz"
                        Command="{Binding BackCommand}"
                        Padding="12,5" />

                <StackPanel Grid.Column="1"
                            HorizontalAlignment="Center">
                    <TextBlock Text="{Binding Title}"
                               FontWeight="SemiBold"
                               TextAlignment="Center" />
                    <TextBlock Text="{Binding LocationText}"
                               Foreground="#6B7280"
                               FontSize="12"
                               TextAlignment="Center" />
                </StackPanel>

                <StackPanel Grid.Column="2"
                            Orientation="Horizontal">
                    <Button Content="Poprzedni"
                            Command="{Binding PreviousCommand}"
                            Padding="12,5"
                            Margin="0,0,8,0" />
                    <Button Content="Następny"
                            Command="{Binding NextCommand}"
                            Padding="12,5" />
                </StackPanel>
            </Grid>
        </Border>

        <Border Grid.Row="1"
                Background="White"
                BorderBrush="#D8DDE6"
                BorderThickness="1"
                Margin="24">
            <FlowDocumentScrollViewer Document="{Binding CurrentDocument}"
                                      VerticalScrollBarVisibility="Auto"
                                      IsToolBarVisible="False" />
        </Border>
    </Grid>
</UserControl>
